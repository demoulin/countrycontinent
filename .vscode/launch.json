{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Test Package",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}",
            "env": {
                "GOFLAGS": "-mod=mod"
            }
        }
    ]
}
